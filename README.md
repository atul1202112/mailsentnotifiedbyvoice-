# mailsentnotifiedbyvoice-


to run it 
install required file 
apt-get install python3-gi
pip install vext

pip install vext.gi
sudo apt-get install pkg-config libcairo2-dev gcc python3-dev libgirepository1.0-dev
pip install gobject PyGObject
